{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<table>
    <tr>
        <td>{{ form_label(form.body, 'Commentaire') }}</td>
        <td>{{ form_widget(form.body) }}</td>
        <td>{{ form_errors(form.body) }}</td>
    </tr>

    <tr>
        <td>{{ form_label(form.status, 'Statut') }}</td>
        <td>{{ form_widget(form.status) }}</td>
        <td>{{ form_errors(form.status) }}</td>
    </tr>

    {# Si tu veux afficher likes (sinon supprime ce bloc) #}
    {# 
    <tr>
        <td>{{ form_label(form.likes, 'Likes') }}</td>
        <td>{{ form_widget(form.likes) }}</td>
        <td>{{ form_errors(form.likes) }}</td>
    </tr>
    #}
</table>

<button class="btn">{{ button_label|default('Enregistrer') }}</button>

{{ form_end(form) }}
