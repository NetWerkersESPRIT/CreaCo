{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<table>
    <tr>
        <td>{{ form_label(form.title, 'Titre') }}</td>
        <td>{{ form_widget(form.title) }}</td>
        <td>{{ form_errors(form.title) }}</td>
    </tr>

    <tr>
        <td>{{ form_label(form.status, 'Statut') }}</td>
        <td>{{ form_widget(form.status) }}</td>
        <td>{{ form_errors(form.status) }}</td>
    </tr>

    <tr>
        <td>{{ form_label(form.pinned, 'Épinglé') }}</td>
        <td>{{ form_widget(form.pinned) }}</td>
        <td>{{ form_errors(form.pinned) }}</td>
    </tr>
</table>

<button class="btn">{{ button_label|default('Enregistrer') }}</button>

{{ form_end(form) }}
