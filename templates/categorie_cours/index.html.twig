{% extends 'base.html.twig' %}

{% block title %}CategorieCours index{% endblock %}

{% block body %}
    <h1>CategorieCours index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_cour in categorie_cours %}
            <tr>
                <td>{{ categorie_cour.id }}</td>
                <td>{{ categorie_cour.nom }}</td>
                <td>{{ categorie_cour.description }}</td>
                <td>
                    <a href="{{ path('app_categorie_cours_show', {'id': categorie_cour.id}) }}">show</a>
                    <a href="{{ path('app_categorie_cours_edit', {'id': categorie_cour.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_cours_new') }}">Create new</a>
{% endblock %}
